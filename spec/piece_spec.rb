# frozen_string_literal: true

require_relative './../lib/piece'

RSpec.describe Piece do
  describe '#moves' do
    context 'when coordinates has a valid move(s)' do
      pending 'Work on instances of Piece first'
      it 'returns an array of valid move positions'
    end
  end
end
